export const LOGIN_TOKEN_STORAGE_KEY = 'Bearer_token';

export const USER_INFO = 'user_info';

export const DOMAIN_URL = process.env.NODE_ENV === 'development' ? 'http://localhost:4011/api' : '/api';
